import 'package:flutter/material.dart';

class SearchInConversas extends StatelessWidget {
  const SearchInConversas({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return IconButton(onPressed: () {}, icon: const Icon(Icons.search));
  }
}
